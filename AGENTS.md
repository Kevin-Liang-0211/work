## Cursor Cloud specific instructions

This repository is currently empty (single `README.md`). There are no application services, dependencies, build steps, lint checks, or tests to run.

When application code is added, update this section with:
- How to install dependencies
- How to run, build, lint, and test the application
- Any non-obvious development caveats
